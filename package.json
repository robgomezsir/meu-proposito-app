{
  "name": "sistema-proposito",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "clsx": "^2.1.1",
    "firebase": "^9.23.0",
    "lucide-react": "^0.268.0",
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "scripts": {
    "set-homepage": "node setHomepage.js",
    "start": "npm run set-homepage && react-scripts start",
    "build": "react-scripts build",
    "build-github": "set \"REACT_APP_ENV=github\" && npm run set-homepage && react-scripts build",
    "build-render": "set \"REACT_APP_ENV=render\" && npm run set-homepage && react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "echo 'Deploy via GitHub Actions - execute: git push origin main'",
    "deploy-gh": "gh-pages -d build --repo https://github.com/robgomezsir/meu-proposito-app.git"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.14",
    "gh-pages": "^6.3.0",
    "postcss": "^8.4.24",
    "react-scripts": "^5.0.1",
    "tailwindcss": "^3.3.0"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "homepage": "."
}